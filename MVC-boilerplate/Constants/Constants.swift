
import UIKit

struct Constants {
    struct UserDefaultsKeys {
        static let loggedUser = "logged_user"
        static let token = "token"
        static let deviceTokenData = "device_token_data"
    }

    struct App {
        static let bundleIdentifier = Bundle.main.bundleIdentifier
    }
}
